import styled from '@emotion/styled'

const RowExpandValueText = styled.p`
  font-family: ${({ theme }) => theme.font.family};
  color: ${({ theme }) => theme.color.white[100]};
  font-style: normal;
  font-weight: normal;
  font-size: 12px;
  line-height: 18px;
  margin: 0;
  padding: 0;
  text-align: left;
`

export default RowExpandValueText
